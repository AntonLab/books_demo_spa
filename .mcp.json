{
  "mcpServers": {
    "playwright": {
      "command": "node",
      "args": [
        "-e",
        "require('node:child_process').spawn('npx @playwright/mcp@latest', { stdio: 'inherit', shell: true }).on('exit', (code) => process.exit(code ?? 1))"
      ]
    }
  }
}
